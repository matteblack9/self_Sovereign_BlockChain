<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="android.blockchain.hackathon">

    <uses-permission android:name="android.permission.USE_FINGERPRINT" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".main.MainActivity" />
        <activity android:name=".SplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainBaseActivity" />
        <activity android:name=".fingerPrint.FingerPrintAuthActivity" />
        <activity android:name=".QRCodeActivity" />
        <activity android:name=".OCRAuthActivity" />
        <activity android:name=".authDetail.AuthDetailActivity" />
        <activity android:name=".TestActivity"></activity>
    </application>

</manifest>